name: Auto Update Sports Channels

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Clear old data
        # هذه الخطوة تحذف الملف القديم لضمان عدم وجود بيانات قديمة
        run: rm -f database.csv

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests cloudscraper

      - name: Run The Beast
        run: python the_beast.py 

      - name: Commit and Push Changes
        run: |
          git config --local user.name "GitHub Action Bot"
          git config --local user.email "actions@github.com"
          git add database.csv
          if git diff --quiet && git diff --staged --quiet; then
            echo "لا توجد تغييرات."
          else
            git commit -m "🔄 تنظيف شامل وتحديث قنوات الرياضة: $(date)"
            git push
          fi
